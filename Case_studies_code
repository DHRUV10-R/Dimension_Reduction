<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dimensionality Reduction Explorer</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="https://cdn.plot.ly/plotly-2.24.1.min.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .fade-in { animation: fadeIn 0.6s ease-in-out; }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .nav-link.active {
      background-color: #0d9488;
      color: white !important;
      border-radius: 8px;
      padding: 6px 12px;
    }
    /* Pills nav improvement */
    .pill {
      transition: all 0.2s;
      border-radius: 9999px;
      padding: 0.5rem 1.25rem;
      font-weight: 500;
      background: linear-gradient(90deg, #14b8a6 0%, #34d399 100%);
      color: white;
      box-shadow: 0 2px 8px rgba(52, 211, 153, 0.08);
      cursor: pointer;
      border: none;
      outline: none;
    }
    .pill:not(.active):hover {
      background: linear-gradient(90deg, #2dd4bf 0%, #6ee7b7 100%);
      color: #065f46;
      box-shadow: 0 4px 16px rgba(52, 211, 153, 0.15);
      transform: translateY(-2px) scale(1.05);
    }
    .pill.active {
      background: linear-gradient(90deg, #0d9488 0%, #059669 100%);
      color: #fff;
      box-shadow: 0 4px 16px rgba(13, 148, 136, 0.15);
      transform: scale(1.08);
    }
    /* Divider */
    .divider {
      height: 2px;
      background: linear-gradient(90deg, #14b8a6 0%, #34d399 100%);
      opacity: 0.15;
      margin: 2rem 0;
      border-radius: 9999px;
    }
    /* Card hover */
    .card-hover:hover {
      box-shadow: 0 8px 32px rgba(52, 211, 153, 0.18);
      transform: translateY(-4px) scale(1.03);
      border: 1px solid #34d399;
    }
    /* Footer icons */
    .footer-icon {
      width: 24px;
      height: 24px;
      vertical-align: middle;
      margin-right: 6px;
      opacity: 0.8;
      transition: opacity 0.2s;
    }
    .footer-link:hover .footer-icon {
      opacity: 1;
    }
    /* Hamburger menu */
    .hamburger {
      display: none;
      flex-direction: column;
      cursor: pointer;
      gap: 4px;
    }
    .hamburger span {
      width: 24px;
      height: 3px;
      background: #0d9488;
      border-radius: 2px;
      transition: all 0.3s;
    }
    @media (max-width: 768px) {
      .hamburger { display: flex; }
      .nav-desktop { display: none !important; }
      .nav-mobile { display: block !important; }
    }
    .nav-mobile {
      display: none;
      position: absolute;
      top: 64px;
      right: 0;
      background: white;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      border-radius: 0 0 12px 12px;
      z-index: 100;
      min-width: 180px;
      padding: 1rem;
    }
    .nav-mobile a {
      display: block;
      margin-bottom: 0.5rem;
      color: #0d9488;
      font-weight: 500;
      padding: 0.5rem 0.75rem;
      border-radius: 8px;
      transition: background 0.2s;
    }
    .nav-mobile a:hover {
      background: #e0f2fe;
      color: #059669;
    }
  </style>
</head>
<body class="antialiased bg-stone-50 text-stone-800">

  <!-- Navbar -->
  <header class="bg-white/80 backdrop-blur-lg sticky top-0 z-50 shadow-sm">
    <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <span class="font-extrabold text-xl bg-gradient-to-r from-teal-600 to-emerald-400 bg-clip-text text-transparent">Dimension Explorer</span>
        </div>
        <div class="nav-desktop hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <a href="#intro" class="nav-link px-3 py-2 text-sm font-medium text-stone-700 hover:bg-stone-100 rounded-lg">Introduction</a>
            <a href="#why" class="nav-link px-3 py-2 text-sm font-medium text-stone-700 hover:bg-stone-100 rounded-lg">Why It Matters</a>
            <a href="#case-studies" class="nav-link px-3 py-2 text-sm font-medium text-stone-700 hover:bg-stone-100 rounded-lg">Case Studies</a>
            <a href="#summary" class="nav-link px-3 py-2 text-sm font-medium text-stone-700 hover:bg-stone-100 rounded-lg">Summary</a>
          </div>
        </div>
        <div class="hamburger md:hidden" id="hamburger-menu">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </div>
      <div class="nav-mobile" id="mobile-nav">
        <a href="#intro">Introduction</a>
        <a href="#why">Why It Matters</a>
        <a href="#case-studies">Case Studies</a>
        <a href="#summary">Summary</a>
      </div>
    </nav>
  </header>

  <main>
    <!-- Hero Section -->
    <section id="intro" class="py-24 bg-gradient-to-br from-teal-50 via-white to-emerald-50 fade-in">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-5xl sm:text-6xl font-extrabold tracking-tight text-stone-900 mb-4">
          An Introduction to <span class="bg-gradient-to-r from-teal-600 to-emerald-400 bg-clip-text text-transparent">Dimensionality Reduction</span> 📉
        </h1>
        <p class="mt-6 max-w-3xl mx-auto text-lg sm:text-xl text-stone-600">Simplify complex data by reducing the number of features—without losing the important information. Discover how this technique powers modern AI and data science.</p>
      </div>
    </section>

    <div class="divider"></div>

    <!-- Why It Matters -->
    <section id="why" class="py-20 sm:py-24 bg-white fade-in">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <h2 class="text-4xl font-bold tracking-tight text-teal-700">Why Does It Matter? 🤔</h2>
          <p class="mt-4 text-lg text-stone-600">High-dimensional data leads to the "Curse of Dimensionality." Reducing dimensions brings key advantages:</p>
        </div>
        <div class="mt-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          <div class="bg-white p-6 rounded-xl shadow-md card-hover transition">
            <div class="text-3xl">🚀</div>
            <h3 class="text-lg font-semibold text-teal-700 mt-2">Improves Model Performance</h3>
            <p class="mt-2 text-stone-600">Reduces overfitting by removing noise and redundancy, helping models generalize better.</p>
          </div>
          <div class="bg-white p-6 rounded-xl shadow-md card-hover transition">
            <div class="text-3xl">⏱️</div>
            <h3 class="text-lg font-semibold text-teal-700 mt-2">Faster Computation</h3>
            <p class="mt-2 text-stone-600">Less data to process → faster training and predictions.</p>
          </div>
          <div class="bg-white p-6 rounded-xl shadow-md card-hover transition">
            <div class="text-3xl">📊</div>
            <h3 class="text-lg font-semibold text-teal-700 mt-2">Better Visualization</h3>
            <p class="mt-2 text-stone-600">Projecting into 2D/3D makes hidden patterns visible.</p>
          </div>
          <div class="bg-white p-6 rounded-xl shadow-md card-hover transition">
            <div class="text-3xl">🗑️</div>
            <h3 class="text-lg font-semibold text-teal-700 mt-2">Reduces Noise</h3>
            <p class="mt-2 text-stone-600">Filters irrelevant features, focusing only on the strongest signals.</p>
          </div>
        </div>
      </div>
    </section>

    <div class="divider"></div>

    <!-- Case Studies -->
    <section id="case-studies" class="py-20 sm:py-24 bg-gradient-to-br from-white to-stone-50 fade-in">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-4xl font-bold tracking-tight text-teal-700">Real-World Case Studies 🌍</h2>
          <p class="mt-4 text-lg text-stone-600">Select a case study below to see dimensionality reduction in action.</p>
        </div>
        <!-- Pills Nav -->
        <div id="case-study-nav" class="flex flex-wrap justify-center gap-3 mb-10"></div>
        <!-- Content -->
        <div id="case-study-content" class="bg-white/80 backdrop-blur-lg p-6 sm:p-8 rounded-2xl shadow-lg min-h-[500px]"></div>
      </div>
    </section>

    <div class="divider"></div>

    <!-- Summary -->
    <section id="summary" class="py-20 sm:py-24 bg-white fade-in">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-4xl font-bold tracking-tight text-teal-700">Summary & Key Takeaways ✅</h2>
        <p class="mt-4 max-w-2xl mx-auto text-lg text-stone-600">Dimensionality reduction is a crucial and versatile technique. Remember:</p>
        <div class="mt-12 max-w-4xl mx-auto space-y-4 text-left">
          <div class="flex items-start bg-stone-50 p-4 rounded-lg shadow-sm">
            <span class="text-teal-600 font-bold mr-3">📌</span>
            <p><span class="font-semibold">More Than Visualization:</span> It boosts efficiency, accuracy, and robustness.</p>
          </div>
          <div class="flex items-start bg-stone-50 p-4 rounded-lg shadow-sm">
            <span class="text-teal-600 font-bold mr-3">🛠️</span>
            <p><span class="font-semibold">Diverse Toolset:</span> PCA, t-SNE, SVD, Autoencoders each solve different problems.</p>
          </div>
          <div class="flex items-start bg-stone-50 p-4 rounded-lg shadow-sm">
            <span class="text-teal-600 font-bold mr-3">🌍</span>
            <p><span class="font-semibold">Foundational Concept:</span> A cornerstone of ML & AI across industries.</p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
<footer class="bg-stone-100 text-stone-600 py-4 text-center text-sm">
  &copy; 2025 Dimensionality Reduction Explorer
</footer>

    <!-- JS for hamburger menu and smooth nav -->
  <script>
    // Hamburger menu toggle
    const hamburger = document.getElementById('hamburger-menu');
    const mobileNav = document.getElementById('mobile-nav');
    hamburger.addEventListener('click', () => {
      mobileNav.style.display = mobileNav.style.display === 'block' ? 'none' : 'block';
    });
    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth' });
          mobileNav.style.display = 'none';
        }
      });
    });
    // Pills nav example (replace with your JS logic)
    const caseStudies = ['PCA', 't-SNE', 'SVD', 'Autoencoder'];
    const nav = document.getElementById('case-study-nav');
    caseStudies.forEach((study, idx) => {
      const btn = document.createElement('button');
      btn.className = 'pill' + (idx === 0 ? ' active' : '');
      btn.innerText = study;
      btn.onclick = () => {
        document.querySelectorAll('.pill').forEach(p => p.classList.remove('active'));
        btn.classList.add('active');
        // Add your content switching logic here
        document.getElementById('case-study-content').innerHTML = `<h3 class="text-2xl font-bold text-teal-700 mb-2">${study} Case Study</h3><p class="text-stone-700">Content for ${study} goes here.</p>`;
      };
      nav.appendChild(btn);
    });
    // Initial content
    document.getElementById('case-study-content').innerHTML = `<h3 class="text-2xl font-bold text-teal-700 mb-2">${caseStudies[0]} Case Study</h3><p class="text-stone-700">Content for ${caseStudies[0]} goes here.</p>`;
  </script>
</body>
</html>